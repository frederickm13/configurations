" Turn syntax highlighting on.
syntax on

" Enable file type detection.
filetype on

" Enable plugins and load plugin for detected file type.
filetype plugin on

" Load an indent file for the detected file type.
filetype indent on

" Add line numbers.
set number

" Set tab size.
set tabstop=4

" Enable auto-indention.
set autoindent

" Replace tabs with whitespace.
set expandtab

" Disable vi compatibility.
set nocompatible

" Highlight matching characters as you type when incrementally searching.
set incsearch

" Ignore casing when searching.
set ignorecase

" Override the ignorecase option when searching for capital letters.
" This will allow you to search specifically for capital letters.
set smartcase

" Show partial command you type in the last line of the screen.
set showcmd

" Show current mode on the last line of the screen.
set showmode

" Use highlighting when doing a search.
set hlsearch

" Show matching words during a search.
set showmatch
